{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"LUIT Oct 2024 Purple","text":""},{"location":"index.html#table-of-contents","title":"Table of Contents","text":"<ol> <li>Code</li> </ol>"},{"location":"index.html#code","title":"Code","text":"<p>This section provides a description of the LUIT Oct 2024 Purple code.</p>"},{"location":"create_instance.html","title":"create_instance Module","text":"<p>Script to create an EC2 instance.</p>"},{"location":"create_instance.html#create_instance.create_instances","title":"<code>create_instances(client, ami_type, instance_amount=1)</code>","text":"<p>Creates EC2 instances based on the specified AMI type.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create instances.</p> required <code>ami_type</code> <code>str</code> <p>The type of AMI to use for the instances (e.g., \"ubuntu\", \"linux 2023\", \"linux 2\").</p> required <code>instance_amount</code> <code>int</code> <p>The number of instances to create. Defaults to 1.</p> <code>1</code> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>create_instance.py</code> <pre><code>def create_instances(client: 'boto3.client', ami_type: str, instance_amount: int = 1) -&gt; None:\n    \"\"\"Creates EC2 instances based on the specified AMI type.\n\n    Args:\n        client (boto3.client): The EC2 client used to create instances.\n        ami_type (str): The type of AMI to use for the instances (e.g., \"ubuntu\", \"linux 2023\", \"linux 2\").\n        instance_amount (int, optional): The number of instances to create. Defaults to 1.\n\n    Returns:\n        None\n    \"\"\"\n    # Loop through the number of instances to create\n    for i in range(instance_amount):\n        # Normalize ami_type by converting to lowercase and removing extra spaces\n        ami = ami_type.lower().strip()\n\n        # Check for Ubuntu AMI type\n        if ami == \"ubuntu\":\n            create_ubuntu_instance(client)  # Create Ubuntu instance\n            print(\"Created Ubuntu Instance\")\n\n        # Check for Amazon Linux 2023 AMI type\n        elif ami == \"linux 2023\":\n            create_amazon_linux_2023_instance(client)  # Create Amazon Linux 2023 instance\n            print(\"Created Linux 2023 Instance\")\n\n        # Check for Amazon Linux 2 AMI type\n        elif ami == \"linux 2\":\n            create_amazon_linux_2_instance(client)  # Create Amazon Linux 2 instance\n            print(\"Created Linux 2 Instance\")\n\n        # Handle invalid AMI type\n        else:\n            print(\"Invalid AMI Type\")\n</code></pre>"},{"location":"data_types.html","title":"data_types Module","text":"<p>Script to test various data types.</p>"},{"location":"data_types.html#data_types.main","title":"<code>main()</code>","text":"<p>Demonstrates type conversions and type checking for various data types.</p> <p>Prompts the user for input, casts types between string, integer, and float, and prints the type of each variable at each step, including the current date and time retrieved using the <code>datetime</code> module.</p> Source code in <code>data_types.py</code> <pre><code>def main() -&gt; None:\n    \"\"\"Demonstrates type conversions and type checking for various data types.\n\n    Prompts the user for input, casts types between string, integer, and float,\n    and prints the type of each variable at each step, including the current\n    date and time retrieved using the `datetime` module.\n    \"\"\"\n    var = \"These are words.\"  # Defining a string variable\n    print(type(var))  # Printing the type of var, which will be &lt;class 'str'&gt;\n\n    var2 = \"386\"  # Another string variable that looks like a number\n    print(type(var2))  # Printing the type of var2, which is also a string &lt;class 'str'&gt;\n\n    var3 = int(var2)  # Converting the string '386' to an integer\n    print(type(var3))  # Printing the type of var3, which will now be &lt;class 'int'&gt;\n\n    var4 = input(\"Enter a number: \")  # Taking user input, which will always return a string\n    print(var4)  # Printing the value entered by the user\n    print(type(var4))  # Printing the type of var4, which will be &lt;class 'str'&gt;\n\n    var5 = float(var4)  # Converting the user input (string) to a float\n    print(var5 + 1)  # Adding 1 to the float value and printing the result\n\n    var6 = datetime.now()  # Getting the current date and time\n    print(var6)  # Printing the current date and time\n    print(type(var6))  # Printing the type of var6, which will be &lt;class 'datetime.datetime'&gt;\n</code></pre>"},{"location":"hello_world.html","title":"hello_world Module","text":"<p>Script to print hello world.</p>"},{"location":"helpers.html","title":"helpers Module","text":"<p>Helper functions to connect to AWS.</p>"},{"location":"helpers.html#helpers.create_amazon_linux_2023_instance","title":"<code>create_amazon_linux_2023_instance(client)</code>","text":"<p>Creates an Amazon Linux 2023 EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_amazon_linux_2023_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Amazon Linux 2023 EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-08a0d1e16fc3f61ea\")  # Call create_instance with the Amazon Linux 2023 AMI ID\n</code></pre>"},{"location":"helpers.html#helpers.create_amazon_linux_2_instance","title":"<code>create_amazon_linux_2_instance(client)</code>","text":"<p>Creates an Amazon Linux 2 EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_amazon_linux_2_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Amazon Linux 2 EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-0eaf7c3456e7b5b68\")  # Call create_instance with the Amazon Linux 2 AMI ID\n</code></pre>"},{"location":"helpers.html#helpers.create_instance","title":"<code>create_instance(client, ami)</code>","text":"<p>Creates an EC2 instance with the specified AMI.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <code>ami</code> <code>str</code> <p>The AMI ID to use for the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_instance(client: boto3.client, ami: str) -&gt; None:\n    \"\"\"Creates an EC2 instance with the specified AMI.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n        ami (str): The AMI ID to use for the instance.\n\n    Returns:\n        None\n    \"\"\"\n    keyName = 'private-ec2'  # Key pair name for the instance\n    client.run_instances(MaxCount=1,\n                         MinCount=1,\n                         ImageId=ami,\n                         InstanceType=\"t2.micro\",\n                         KeyName=keyName,\n                         SecurityGroupIds=['sg-0197b8159a5d886f8'])  # Run the instance with specified parameters\n</code></pre>"},{"location":"helpers.html#helpers.create_ubuntu_instance","title":"<code>create_ubuntu_instance(client)</code>","text":"<p>Creates an Ubuntu EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_ubuntu_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Ubuntu EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-04b70fa74e45c3917\")  # Call create_instance with the Ubuntu AMI ID\n</code></pre>"},{"location":"helpers.html#helpers.describe_instances","title":"<code>describe_instances(client)</code>","text":"<p>Describes EC2 instances and returns a list of instances.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to describe instances.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of instances.</p> Source code in <code>helpers.py</code> <pre><code>def describe_instances(client: boto3.client) -&gt; list:\n    \"\"\"Describes EC2 instances and returns a list of instances.\n\n    Args:\n        client (boto3.client): The EC2 client used to describe instances.\n\n    Returns:\n        list: A list of instances.\n    \"\"\"\n    response = client.describe_instances()  # Call the describe_instances method to get information about EC2 instances\n    instances = []\n    for reservation in response['Reservations']:  # Iterate over each reservation in the response\n        instances.extend(reservation['Instances'])  # Extend the instances list with instances from the reservation\n    return instances\n</code></pre>"},{"location":"helpers.html#helpers.get_ec2_client","title":"<code>get_ec2_client()</code>","text":"<p>Creates and returns an EC2 client using Boto3.</p> <p>Returns:</p> Type Description <code>client</code> <p>boto3.client: The EC2 client.</p> Source code in <code>helpers.py</code> <pre><code>def get_ec2_client() -&gt; boto3.client:\n    \"\"\"Creates and returns an EC2 client using Boto3.\n\n    Returns:\n        boto3.client: The EC2 client.\n    \"\"\"\n    return boto3.client('ec2')\n</code></pre>"},{"location":"helpers.html#helpers.get_s3_client","title":"<code>get_s3_client()</code>","text":"<p>Creates and returns an S3 client using Boto3.</p> <p>Returns:</p> Type Description <code>client</code> <p>boto3.client: The S3 client.</p> Source code in <code>helpers.py</code> <pre><code>def get_s3_client() -&gt; boto3.client:\n    \"\"\"Creates and returns an S3 client using Boto3.\n\n    Returns:\n        boto3.client: The S3 client.\n    \"\"\"\n    return boto3.client('s3')\n</code></pre>"},{"location":"helpers.html#helpers.list_buckets","title":"<code>list_buckets(s3_client)</code>","text":"<p>Lists the names of all S3 buckets.</p> <p>Parameters:</p> Name Type Description Default <code>s3_client</code> <code>client</code> <p>The S3 client used to list buckets.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of bucket names.</p> Source code in <code>helpers.py</code> <pre><code>def list_buckets(s3_client: boto3.client) -&gt; list:\n    \"\"\"Lists the names of all S3 buckets.\n\n    Args:\n        s3_client (boto3.client): The S3 client used to list buckets.\n\n    Returns:\n        list: A list of bucket names.\n    \"\"\"\n    response = s3_client.list_buckets()  # Call the list_buckets method to get information about S3 buckets\n    return [bucket['Name'] for bucket in response['Buckets']]  # Extract and return the list of bucket names\n</code></pre>"},{"location":"imports.html","title":"imports Module","text":"<p>Script to test various imports.</p>"},{"location":"list_buckets.html","title":"list_buckets Module","text":"<p>Script to list bucket names from AWS S3.</p>"},{"location":"list_buckets_lambda.html","title":"list_buckets_lambda Module","text":"<p>Lambda to list buckets from S3.</p>"},{"location":"list_buckets_lambda.html#list_buckets_lambda.lambda_handler","title":"<code>lambda_handler(event, context)</code>","text":"<p>AWS Lambda handler function to list all S3 bucket names.</p> <p>This function retrieves the names of all S3 buckets in the AWS account and returns them in the response body as a newline-separated string.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Dict[str, Any]</code> <p>The event triggering the Lambda function.</p> required <code>context</code> <code>Any</code> <p>Contextual information provided by AWS Lambda.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: A dictionary containing the HTTP status code and</p> <code>Dict[str, Any]</code> <p>the body with bucket names as a newline-separated string.</p> Source code in <code>list_buckets_lambda.py</code> <pre><code>def lambda_handler(event: Dict[str, Any], context: Any) -&gt; Dict[str, Any]:\n    \"\"\"AWS Lambda handler function to list all S3 bucket names.\n\n    This function retrieves the names of all S3 buckets in the AWS account\n    and returns them in the response body as a newline-separated string.\n\n    Args:\n        event (Dict[str, Any]): The event triggering the Lambda function.\n        context (Any): Contextual information provided by AWS Lambda.\n\n    Returns:\n        Dict[str, Any]: A dictionary containing the HTTP status code and\n        the body with bucket names as a newline-separated string.\n    \"\"\"\n    # Initialize S3 client\n    s3 = boto3.client('s3')\n\n    # List all S3 buckets\n    response = s3.list_buckets()\n\n    # Extract bucket list from response\n    buckets = response['Buckets']\n\n    # Initialize list to store bucket names\n    bucket_names = []\n\n    # Iterate through each bucket and append its name to bucket_names list\n    for bucket in buckets:\n        print(bucket['Name'])  # Log each bucket name for debugging\n        bucket_names.append(bucket['Name'])\n\n    # Return the response with status code and joined bucket names\n    return {\n        'statusCode': 200,\n        'body': '\\n'.join(bucket_names)\n    }\n</code></pre>"},{"location":"list_cron_expressions.html","title":"list_cron_expressions Module","text":"<p>Listing Schedule Expressions from AWS EventBridge.</p>"},{"location":"list_resources.html","title":"list_resources Module","text":"<p>Script to list AWS resources.</p>"},{"location":"list_resources.html#list_resources.create_amazon_linux_2023_instance","title":"<code>create_amazon_linux_2023_instance(client)</code>","text":"<p>Creates an Amazon Linux 2023 EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_amazon_linux_2023_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Amazon Linux 2023 EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-08a0d1e16fc3f61ea\")  # Call create_instance with the Amazon Linux 2023 AMI ID\n</code></pre>"},{"location":"list_resources.html#list_resources.create_amazon_linux_2_instance","title":"<code>create_amazon_linux_2_instance(client)</code>","text":"<p>Creates an Amazon Linux 2 EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_amazon_linux_2_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Amazon Linux 2 EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-0eaf7c3456e7b5b68\")  # Call create_instance with the Amazon Linux 2 AMI ID\n</code></pre>"},{"location":"list_resources.html#list_resources.create_instance","title":"<code>create_instance(client, ami)</code>","text":"<p>Creates an EC2 instance with the specified AMI.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <code>ami</code> <code>str</code> <p>The AMI ID to use for the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_instance(client: boto3.client, ami: str) -&gt; None:\n    \"\"\"Creates an EC2 instance with the specified AMI.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n        ami (str): The AMI ID to use for the instance.\n\n    Returns:\n        None\n    \"\"\"\n    keyName = 'private-ec2'  # Key pair name for the instance\n    client.run_instances(MaxCount=1,\n                         MinCount=1,\n                         ImageId=ami,\n                         InstanceType=\"t2.micro\",\n                         KeyName=keyName,\n                         SecurityGroupIds=['sg-0197b8159a5d886f8'])  # Run the instance with specified parameters\n</code></pre>"},{"location":"list_resources.html#list_resources.create_ubuntu_instance","title":"<code>create_ubuntu_instance(client)</code>","text":"<p>Creates an Ubuntu EC2 instance.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to create the instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>helpers.py</code> <pre><code>def create_ubuntu_instance(client: boto3.client) -&gt; None:\n    \"\"\"Creates an Ubuntu EC2 instance.\n\n    Args:\n        client (boto3.client): The EC2 client used to create the instance.\n\n    Returns:\n        None\n    \"\"\"\n    create_instance(client, \"ami-04b70fa74e45c3917\")  # Call create_instance with the Ubuntu AMI ID\n</code></pre>"},{"location":"list_resources.html#list_resources.describe_instances","title":"<code>describe_instances(client)</code>","text":"<p>Describes EC2 instances and returns a list of instances.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>client</code> <p>The EC2 client used to describe instances.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of instances.</p> Source code in <code>helpers.py</code> <pre><code>def describe_instances(client: boto3.client) -&gt; list:\n    \"\"\"Describes EC2 instances and returns a list of instances.\n\n    Args:\n        client (boto3.client): The EC2 client used to describe instances.\n\n    Returns:\n        list: A list of instances.\n    \"\"\"\n    response = client.describe_instances()  # Call the describe_instances method to get information about EC2 instances\n    instances = []\n    for reservation in response['Reservations']:  # Iterate over each reservation in the response\n        instances.extend(reservation['Instances'])  # Extend the instances list with instances from the reservation\n    return instances\n</code></pre>"},{"location":"list_resources.html#list_resources.get_ec2_client","title":"<code>get_ec2_client()</code>","text":"<p>Creates and returns an EC2 client using Boto3.</p> <p>Returns:</p> Type Description <code>client</code> <p>boto3.client: The EC2 client.</p> Source code in <code>helpers.py</code> <pre><code>def get_ec2_client() -&gt; boto3.client:\n    \"\"\"Creates and returns an EC2 client using Boto3.\n\n    Returns:\n        boto3.client: The EC2 client.\n    \"\"\"\n    return boto3.client('ec2')\n</code></pre>"},{"location":"list_resources.html#list_resources.get_s3_client","title":"<code>get_s3_client()</code>","text":"<p>Creates and returns an S3 client using Boto3.</p> <p>Returns:</p> Type Description <code>client</code> <p>boto3.client: The S3 client.</p> Source code in <code>helpers.py</code> <pre><code>def get_s3_client() -&gt; boto3.client:\n    \"\"\"Creates and returns an S3 client using Boto3.\n\n    Returns:\n        boto3.client: The S3 client.\n    \"\"\"\n    return boto3.client('s3')\n</code></pre>"},{"location":"list_resources.html#list_resources.list_buckets","title":"<code>list_buckets(s3_client)</code>","text":"<p>Lists the names of all S3 buckets.</p> <p>Parameters:</p> Name Type Description Default <code>s3_client</code> <code>client</code> <p>The S3 client used to list buckets.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of bucket names.</p> Source code in <code>helpers.py</code> <pre><code>def list_buckets(s3_client: boto3.client) -&gt; list:\n    \"\"\"Lists the names of all S3 buckets.\n\n    Args:\n        s3_client (boto3.client): The S3 client used to list buckets.\n\n    Returns:\n        list: A list of bucket names.\n    \"\"\"\n    response = s3_client.list_buckets()  # Call the list_buckets method to get information about S3 buckets\n    return [bucket['Name'] for bucket in response['Buckets']]  # Extract and return the list of bucket names\n</code></pre>"},{"location":"list_resources.html#list_resources.print_bucket_names","title":"<code>print_bucket_names(s3_client)</code>","text":"<p>Prints the names of all S3 buckets associated with the provided S3 client.</p> <p>Parameters:</p> Name Type Description Default <code>s3_client</code> <code>object</code> <p>The S3 client used to list and access S3 buckets.</p> required Source code in <code>list_resources.py</code> <pre><code>def print_bucket_names(s3_client: object) -&gt; None:\n    \"\"\"Prints the names of all S3 buckets associated with the provided S3 client.\n\n    Args:\n        s3_client (object): The S3 client used to list and access S3 buckets.\n    \"\"\"\n    bucket_names = list_buckets(s3_client)\n\n    # Print elements of the list. Could also be done with '\\n'.join(bucket_names)\n    for bucket_name in bucket_names:\n        print(bucket_name)\n</code></pre>"},{"location":"list_resources.html#list_resources.print_instance_ids","title":"<code>print_instance_ids(ec2_client)</code>","text":"<p>Prints the instance IDs of all EC2 instances associated with the provided EC2 client.</p> <p>Parameters:</p> Name Type Description Default <code>ec2_client</code> <code>object</code> <p>The EC2 client used to describe EC2 instances.</p> required Source code in <code>list_resources.py</code> <pre><code>def print_instance_ids(ec2_client: object) -&gt; None:\n    \"\"\"Prints the instance IDs of all EC2 instances associated with the provided EC2 client.\n\n    Args:\n        ec2_client (object): The EC2 client used to describe EC2 instances.\n    \"\"\"\n    instances = describe_instances(ec2_client)\n\n    instance_ids = []\n    # Collect instance IDs. Could print instance['InstanceId'] directly to avoid this list.\n    for instance in instances:\n        instance_ids.append(instance['InstanceId'])\n\n    # Print instance IDs. Could have printed directly in the loop above.\n    for instance_id in instance_ids:\n        print(instance_id)\n</code></pre>"},{"location":"vpc_ids.html","title":"vpc_ids Module","text":"<p>Script for printing VPC Ids from AWS.</p>"}]}